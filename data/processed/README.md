Generated-data folder.
`student_features.csv` and `training_labels.csv` are included as reference outputs. "
"Running `python src/prepare_data.py` with PyArrow installed also creates the Parquet files required by Feast.
